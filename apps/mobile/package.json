{
  "name": "mobile",
  "version": "0.0.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "build:ios": "eas build --platform ios --profile $EAS_PROFILE",
    "build:android": "eas build --platform android --profile $EAS_PROFILE"
  },
  "dependencies": {
    "@factory/core": "workspace:*",
    "@factory/themes": "workspace:*",
    "expo": "~51.0.0",
    "react": "^18.3.0",
    "react-native": "0.74.0"
  }
}
